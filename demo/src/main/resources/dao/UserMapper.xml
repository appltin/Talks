<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talks.demo.articleDao.dao.UserMapper">

    <select id="getAvatarType" parameterType="String" resultType="user">
        select id as userId, avatar from user where username = #{username}
    </select>

    <insert id="register" parameterType="user">
        INSERT INTO user (username, password, role, enabled, avatar)
        VALUES (#{username}, #{password}, #{role}, #{enabled}, #{avatar});
    </insert>

    <select id="searchKeyWord" parameterType="String" resultType="article">
        SELECT * FROM article
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        OR content LIKE CONCAT('%', #{keyword}, '%')
    </select>
    
    <insert id="addArticle" parameterType="article">
        insert into article (title, user_id, username, content, border)
        values(#{title}, #{userId}, #{username}, #{content}, #{border})
    </insert>


</mapper>